package com.solarsentinel.widget

import android.content.Context

// Temporary placeholder for Task 7 so RefreshWorker compiles.
// Task 8 replaces this with the real Glance widget (companion-object form).
object SolarWidget {
  suspend fun updateAll(context: Context) {}
}
